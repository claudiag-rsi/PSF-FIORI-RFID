# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: fw.flexwarehouse
type: application
server:
  customMiddleware:
    - name: fiori-tools-proxy
      afterMiddleware: compression
      configuration:
        ignoreCertErrors: false # If set to true, certificate errors will be ignored. E.g. self-signed certificates will be accepted
        ui5:
          path:
            - /resources
            - /test-resources
          url: https://ui5.sap.com
        backend:
          # Proxy para desarrollo local usando /sap
          - path: /sap
            url: http://fapdvw04:1443
            destination: s4hanadev
          # Proxy para desarrollo local usando /destinations/s4hanadev
          - path: /destinations/s4hanadev
            url: http://fapdvw04:1443
            destination: s4hanadev
            rewrite: true # reescribe la ruta para que funcione local
    - name: fiori-tools-appreload
      afterMiddleware: compression
      configuration:
        port: 35729
        path: webapp
        delay: 300
    - name: fiori-tools-preview
      afterMiddleware: fiori-tools-appreload
      configuration:
        flp:
          theme: sap_horizon
