<Dialog
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    title="{i18n>modalNamePrint}"
    id="PrintDialog"
    stretchOnPhone="true"
    class="customDialog customFont"
>
    <content>
        <VBox
            id="vBoxMainPrint"
            class="sapUiSmallMargin customVBoxPrint"
            width="100%"
        >
            <!-- Código de Producto* -->
            <VBox id="vBoxProductCode">
                <Label
                    id="lblProductCode"
                    text="{i18n>columnProductCode}*"
                    class="customLabel customFont"
                />
                <ComboBox
                    id="cbxProductCode"
                    class="customInput customSelect customFont"
                    selectedKey="{ProductModel>/Productcode}"
                    change="onProductChange"
                    showSecondaryValues="true"
                    filterSecondaryValues="true"
                    items="{ path: 'ProductModel>/' }"
                >
                    <core:ListItem
                        id="colProductCode"
                        key="{ProductModel>Matnr}"
                        text="{ProductModel>Description}"
                    />
                </ComboBox>
            </VBox>

            <!-- Linea de Producción* -->
            <VBox id="vBoxProductionLines">
                <Label
                    id="lblProductionLine"
                    text="{i18n>columnProductionLine}*"
                    class="customLabel customFont"
                />

                <ComboBox
                    id="cbxProductionLines"
                    class="customInput customSelect customFont"
                    selectedKey="{ProductionLineModel>/selectedKey}"
                    change="onProductChange"
                    showSecondaryValues="true"
                    filterSecondaryValues="true"
                    items="{ path: 'ProductionLineModel>/' }"
                >
                    <core:ListItem
                        id="colProductionLines"
                        key="{ProductionLineModel>Arbpl}"
                        text="{ProductionLineModel>Description}"
                    />
                </ComboBox>
            </VBox>

            <!-- Producto* -->
            <VBox
                class="full-width-second-row"
                id="vBoxProduct"
            >
                <Label
                    id="lblProduct"
                    text="{i18n>columnProduct}*"
                    class="customLabel customFont"
                />
                <Text
                    id="txtProduct"
                    text="ProductModel>/Product"
                    class="customInput customText-noDrop customFont"
                />
            </VBox>

            <!-- Centro* -->
            <VBox id="vBoxCenter">
                <Label
                    id="lblCenter"
                    text="{i18n>columnCenter}*"
                    class="customLabel customFont"
                />
                <ComboBox
                    id="cbxCenters"
                    class="customInput customSelect customFont"
                    selectedKey="{centersModel>/selectedKey}"
                    showSecondaryValues="true"
                    filterSecondaryValues="true"
                    items="{ path: 'centersModel>/' }"
                >
                    <core:ListItem
                        id="colCenters"
                        key="{centersModel>Werks}"
                        text="{centersModel>Description}"
                    />
                </ComboBox>
            </VBox>

            <!--Cantidad de Pallets* -->
            <VBox id="vBoxQuantityPallets">
                <Label
                    id="lblQuantityPallets"
                    text="{i18n>columnQuantityPallets}*"
                    class="customLabel customFont"
                />
                <Input
                    id="inpQuantityPallets"
                    placeholder="{i18n>columnQuantityPallets}"
                    class="customInput customFont"
                />
            </VBox>

            <!--Cantidad de cajas* -->
            <VBox id="vBoxBoxesNumber">
                <Label
                    id="lblBoxesNumber"
                    text="{i18n>columnNumberBoxes}*"
                    class="customLabel customFont"
                />
                <Input
                    id="inpBoxesNumber"
                    placeholder="{i18n>columnNumberBoxes}"
                    type="Number"
                    change="onValueChange"
                    class="customInput customFont"
                />
            </VBox>

            <!-- Emblistado -->
            <VBox
                id="vBoxEmbilted"
                class="row4"
            >
                <Label
                    id="lblEmbilted"
                    text="{i18n>columnEmbilted}"
                    class="customLabel customFont"
                />
                <Input
                    id="inpEmbilted"
                    placeholder="{i18n>columnEmbilted}"
                    class="customInput customFont"
                />
            </VBox>
        </VBox>
    </content>

    <beginButton>
        <Button
            id="btnAcceptPrint"
            text="{i18n>save}"
            press=".onSave"
            type="Accept"
            class="btnDialogAccept"
        />
    </beginButton>

    <endButton>
        <Button
            id="btnClosePrint"
            text="{i18n>close}"
            press=".onCancel"
            type="Reject"
            class="btnDialogCancel"
        />
    </endButton>
</Dialog>
